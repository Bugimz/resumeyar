# ResumeYar

یک اپلیکیشن فلاتر برای ساخت رزومه آفلاین با GetX و SQLite که خروجی PDF چندزبانه تولید می‌کند. این نسخه شامل لایه طلایی (Gold) با پرداخت درون‌برنامه‌ای بازار است.

## ویژگی‌ها
- 📝 مدیریت رزومه آفلاین: پروفایل، سوابق کاری، تحصیلات، مهارت‌ها و پروژه‌ها (SQLite).
- 🌐 دو زبانه و RTL: فارسی/انگلیسی با تم تاریک/روشن.
- 🖨️ خروجی PDF با فونت فارسی و سه قالب: Minimal، Modern، و **Elegant (طلایی)**.
- 🗂️ مرتب‌سازی بخش‌های رزومه برای PDF.
- 💾 بک‌آپ/ری‌استور آفلاین (JSON).
- 🎨 تصویر پروفایل و استایل هدر در PDF.
- 🔒 **پرداخت درون‌برنامه‌ای بازار** برای فعال‌سازی نسخه Gold.

## نسخه رایگان در مقابل نسخه طلایی
| قابلیت | رایگان | طلایی (اشتراک/لایسنس) |
| --- | --- | --- |
| ایجاد و ویرایش تمام بخش‌های رزومه | ✅ | ✅ |
| خروجی PDF قالب‌های Minimal و Modern | ✅ | ✅ |
| قالب Elegant با هدر گرادیان و استایل ویژه | ❌ | ✅ |
| بک‌آپ و بازیابی آفلاین (JSON) | ❌ | ✅ |
| بنر تبلیغی ارتقا | ✅ | — |

## راه‌اندازی
1. وابستگی‌ها را دریافت کنید:
   ```bash
   flutter pub get
   ```
2. بسته‌های بومی (اندروید) را طبق دستورالعمل بازار همگام کنید؛ فایل `android/app/build.gradle` باید مخزن کافه‌بازار را داشته باشد و نسخه minSdk 21+ باشد.
3. محصول غیرمصرفی Gold را در پنل کافه‌بازار با شناسه `resumeyar_premium` بسازید.
4. اپ را اجرا کنید:
   ```bash
   flutter run
   ```

## فعال‌سازی پرداخت درون‌برنامه‌ای بازار
- از پلاگین `in_app_purchase_bazaar` استفاده شده است. در `main.dart` سرویس `PremiumService` هنگام بوت اپ مقداردهی می‌شود تا به استور متصل شود و خریدها را بازیابی کند.
- سرویس `BillingService` شناسه محصول `resumeyar_premium` را استعلام می‌گیرد و خرید غیرمصرفی انجام می‌دهد. پس از خرید یا بازیابی، پرچم پریمیوم در `SharedPreferences` ذخیره می‌شود.
- بک‌آپ و قالب Elegant تنها در صورت فعال بودن Gold قابل استفاده هستند؛ اگر کاربر ارتقا نداده باشد دیالوگ ارتقا نمایش داده می‌شود.

## مسیرهای کلیدی
- `lib/app/services/billing_service.dart` و `premium_service.dart`: اتصال به بازار و مدیریت حالت طلایی.
- `lib/app/modules/home/home_view.dart`: انتخاب قالب PDF و بنر ارتقا.
- `lib/app/modules/settings/settings_view.dart`: بک‌آپ/ری‌استور (گیت پریمیوم) و تغییر ترتیب بخش‌ها.
- `lib/app/services/pdf_service.dart`: سه قالب PDF و استایل هدرها.

## نکات پیاده‌سازی بازار
- قبل از انتشار، بسته امضا (keystore) و شناسه بسته را در پنل بازار ثبت کنید.
- تست پرداخت را با حساب تست بازار انجام دهید تا اطمینان حاصل شود پاسخ خرید در `purchaseStream` دریافت می‌شود.
- در صورت نیاز به لاگ بیشتر می‌توانید `purchaseUpdates` را در `PremiumService` مانیتور کنید.

موفق باشید! ✨
